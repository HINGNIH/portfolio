import { Menu } from './';
export const Header = () => {
    return (
        <header id='header'>
            <div className='inner'>
                <Menu />
            </div>
        </header>
    );
};
